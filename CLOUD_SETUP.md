# ☁️ Синхронізація звіту з Google-таблицею

Щоб дані звіту зберігалися в одній спільній хмарній таблиці й були доступні
з **будь-якого комп'ютера чи телефону** (а не лише в одному браузері), треба
один раз налаштувати міст між звітом і Google-таблицею. Це безкоштовно й не
потребує окремого сервера.

Схема: **Звіт (браузер)** ⇄ **Google Apps Script (веб-застосунок)** ⇄ **Google-таблиця**.

---

## Крок 1. Створити таблицю

1. Відкрийте <https://sheets.google.com> і створіть нову порожню таблицю.
2. Назвіть її, напр., `L-TEX SMM — база`.

## Крок 2. Додати скрипт

1. У таблиці: меню **Розширення → Apps Script**.
2. Видаліть увесь код, що там є, і вставте вміст файлу **`google-apps-script.gs`** з цього репозиторію.
3. Знайдіть рядок:
   ```js
   const SECRET = 'ltex-2026';
   ```
   і замініть `ltex-2026` на власний секретний ключ (будь-який рядок, напр. `ltex-secret-7391`). **Запам'ятайте його** — він знадобиться у звіті.
4. Натисніть 💾 (Зберегти проєкт).

## Крок 3. Розгорнути як веб-застосунок

1. Угорі праворуч: **Розгорнути → Нове розгортання**.
2. Тип (шестерня «Виберіть тип») → **Веб-застосунок**.
3. Параметри:
   - **Виконувати як:** Я (ваш акаунт).
   - **Хто має доступ:** **Усі** (Anyone).
4. **Розгорнути**. Google попросить дозвіл — підтвердіть (Advanced → Go to project → Allow).
5. Скопіюйте **URL веб-застосунку** — він виглядає так:
   ```
   https://script.google.com/macros/s/AKfyc..…../exec
   ```

> ⚠️ Доступ «Усі» означає, що URL — публічний, але дані захищені **секретним ключем**: без правильного ключа скрипт нічого не віддає й не пише. Тримайте URL і ключ приватними.

## Крок 4. Підключити у звіті

1. Відкрийте звіт (`smm_report.html`).
2. Угорі натисніть кнопку **☁️**.
3. Вставте:
   - **URL веб-застосунку** — з кроку 3.
   - **Секретний ключ** — той самий, що в скрипті (крок 2.3).
   - Галочку **«Автосинхронізація при збереженні»** лишіть увімкненою.
4. **Зберегти**. Звіт одразу зробить першу синхронізацію.

Готово. Тепер при кожному збереженні дані вивантажуються в таблицю, а при
відкритті звіту на будь-якому пристрої — підтягуються з неї.

---

## Як це працює / що варто знати

- **Офлайн-режим лишається.** Звіт і далі працює без інтернету через локальний
  кеш (localStorage). Синхронізація лише додає хмарну копію.
- **Злиття даних.** При синхронізації щоденні звіти об'єднуються за датами
  (дані з різних пристроїв не затирають одне одного). Трекери, цілі —
  за принципом «останній запис перемагає» (новіший за часом).
- **Оновити нову версію коду скрипта:** Розгорнути → **Керувати розгортаннями**
  → олівець (редагувати) → Версія: «Нова версія» → Розгорнути. URL не зміниться.
- **Кілька людей / пристроїв** можуть користуватися однією таблицею — вкажіть їм
  той самий URL і ключ.

## Якщо щось не працює

| Симптом | Причина / рішення |
|---------|-------------------|
| «Помилка синхр.» одразу | Перевірте URL (має закінчуватися на `/exec`) і що ключ збігається зі скриптом. |
| `unauthorized` | Ключ у звіті ≠ `SECRET` у скрипті. |
| Порожньо після зміни коду | Не забули «Нове розгортання»/«Нова версія» після редагування скрипта. |
| Дані не тягнуться на іншому пристрої | Той самий URL і ключ? Натисніть ☁️ → «Синхронізувати зараз». |
